apiVersion: apps/v1
kind: Deployment
metadata:
  name: microservice-hub
  labels:
    app: microservice-hub
spec:
  replicas: 3
  selector:
    matchLabels:
      app: microservice-hub
  template:
    metadata:
      labels:
        app: microservice-hub
    spec:
      containers:
      - name: api
        image: microservice-hub:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:postgres@db:5432/app"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
