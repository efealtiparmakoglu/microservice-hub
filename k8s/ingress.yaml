apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservice-hub-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: microservice-hub.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: microservice-hub-service
            port:
              number: 80
